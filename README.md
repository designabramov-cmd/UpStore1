# UpStore v2 - Modern E-commerce Platform

Современная платформа интернет-магазина на Next.js 14 с Prisma, TanStack Query, Zustand и Tailwind CSS.

## 🚀 Стек технологий

| Технология | Назначение |
|------------|------------|
| **Next.js 14** | App Router, SSR, API Routes |
| **React 18** | UI Framework |
| **TypeScript** | Типизация |
| **Prisma** | ORM для PostgreSQL |
| **PostgreSQL** | База данных |
| **TanStack Query** | Кэширование и синхронизация |
| **Zustand** | State management |
| **NextAuth.js** | Аутентификация |
| **Tailwind CSS** | Стили |
| **Radix UI** | Headless UI компоненты |
| **Zod** | Валидация |

## 📋 Требования

- Node.js 18+ 
- PostgreSQL база данных (можно использовать бесплатные облачные сервисы)

### Рекомендуемые бесплатные PostgreSQL провайдеры:

- **[Neon](https://neon.tech)** - 0.5GB бесплатно
- **[Supabase](https://supabase.com)** - 500MB бесплатно  
- **[Railway](https://railway.app)** - $5 бесплатного кредита

## 🛠️ Установка

### 1. Клонирование и установка зависимостей

```bash
# Распакуйте архив
unzip upstore-v2.zip
cd upstore-v2

# Установите зависимости
npm install
```

### 2. Настройка базы данных

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
# PostgreSQL (пример для Neon)
DATABASE_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"

# NextAuth (сгенерируйте случайный ключ)
NEXTAUTH_SECRET="your-secret-key-here"
NEXTAUTH_URL="http://localhost:3000"
```

### 3. Инициализация базы данных

```bash
# Создать таблицы
npm run db:push

# Заполнить начальными данными
npm run db:seed
```

### 4. Запуск

```bash
# Режим разработки
npm run dev

# Сборка для продакшена
npm run build
npm start
```

Откройте [http://localhost:3000](http://localhost:3000)

## 🔐 Админ-панель

- URL: [http://localhost:3000/admin](http://localhost:3000/admin)
- Email: `admin@upstore.ru`
- Пароль: `admin123`

## 📁 Структура проекта

```
upstore-v2/
├── app/                    # Next.js App Router
│   ├── api/               # API Routes
│   │   ├── admin/        # Защищённые админ API
│   │   ├── auth/         # NextAuth
│   │   ├── products/     # Публичные API
│   │   └── ...
│   ├── admin/            # Админ-панель
│   ├── product/[id]/     # Страница товара
│   ├── layout.tsx        # Корневой layout
│   ├── page.tsx          # Главная страница
│   └── globals.css       # Глобальные стили
├── components/
│   ├── ui/               # shadcn/ui компоненты
│   ├── layout/           # Header, Footer
│   ├── home/             # Компоненты главной
│   ├── product/          # Компоненты товара
│   └── admin/            # Компоненты админки
├── hooks/                 # React Query hooks
├── stores/                # Zustand stores
├── lib/
│   ├── db.ts             # Prisma client
│   ├── auth.ts           # NextAuth config
│   ├── utils.ts          # Утилиты
│   └── validations.ts    # Zod схемы
├── prisma/
│   ├── schema.prisma     # Схема БД
│   └── seed.ts           # Начальные данные
└── public/
    ├── uploads/          # Загруженные файлы
    └── src/              # Статические ресурсы
```

## 🔧 Команды

```bash
npm run dev          # Запуск в режиме разработки
npm run build        # Сборка для продакшена
npm start            # Запуск продакшен сборки

npm run db:push      # Применить схему к БД
npm run db:migrate   # Создать миграцию
npm run db:studio    # Открыть Prisma Studio
npm run db:seed      # Заполнить тестовыми данными
```

## 🌐 Деплой

### Vercel (рекомендуется)

1. Подключите репозиторий к Vercel
2. Добавьте переменные окружения:
   - `DATABASE_URL` - строка подключения к PostgreSQL
   - `NEXTAUTH_SECRET` - секретный ключ
   - `NEXTAUTH_URL` - URL вашего сайта
3. Vercel автоматически выполнит `prisma generate` при деплое

### Railway / Render / VPS

```bash
# Установка
npm install

# Сборка
npm run build

# Запуск
npm start
```

## 📱 Функции

- ✅ SSR и SEO оптимизация
- ✅ Адаптивный дизайн (mobile-first)
- ✅ Тёмная тема с glassmorphism эффектами
- ✅ Каталог с фильтрацией по категориям
- ✅ Поиск по товарам
- ✅ Слайдер баннеров
- ✅ Бегущая строка логотипов
- ✅ Страницы товаров с опциями
- ✅ Выбор цвета с превью изображения
- ✅ Админ-панель с аутентификацией
- ✅ CRUD для товаров, категорий, баннеров
- ✅ Загрузка изображений
- ✅ Валидация форм

## 🔒 Безопасность

- Все админ-роуты защищены NextAuth.js middleware
- Пароли хэшируются с bcrypt
- API валидирует все входные данные с Zod
- CSRF защита через NextAuth

## 📄 Лицензия

MIT

---

Создано с ❤️ для UpStore
